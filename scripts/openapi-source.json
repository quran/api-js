{
  "$comment": [
    "Pinned source revision for the generated operation catalogs.",
    "",
    "The catalogs are a published artifact: the paths and scopes in them are what the SDK",
    "requests at runtime. Reading them from a moving branch means a rebuild can change client",
    "behaviour with no corresponding change in this repository, and it makes CI non-reproducible.",
    "So generation, `--check`, and CI all read this one file.",
    "",
    "`ref` must be a full 40-character commit SHA. The generator warns loudly if it is a branch",
    "name, because a branch is not a reproducible input.",
    "",
    "To update: point `ref` at the approved qf-api-docs commit, run",
    "`pnpm --filter @quranjs/api generate:operation-catalogs`, and commit both together.",
    "",
    "TODO: this currently points at the head of qf-api-docs `feat/content-scope-split-v1`,",
    "which is the revision carrying the `x-qf-scopes` content scope metadata. Once",
    "quran/qf-api-docs#207 merges, move this to the resulting commit on `main`."
  ],
  "repository": "quran/qf-api-docs",
  "ref": "63de0591c7336b73108932505fc2d09c35a07e0e",
  "path": "openAPI"
}

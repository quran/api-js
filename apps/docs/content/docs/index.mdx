---
title: Introduction
description: A library for fetching quran data from the Quran.com API on both Node.js and the browser.
---

## What is QuranJS?

QuranJS is a library for fetching quran data from the Quran.com API on both Node.js and the browser.

## Installation

<Tabs items={["npm", "pnpm", "yarn", "bun"]}>

  <Tab>
  ```bash 
  npm install @quranjs/api 
  ```
  </Tab>
  <Tab>
  ```bash 
  pnpm install @quranjs/api 
  ```
  </Tab>
  <Tab>
  ```bash 
  yarn add @quranjs/api 
  ```
  </Tab>
  <Tab>
  ```bash 
  bun add @quranjs/api 
  ```
  </Tab>
</Tabs>

<AutoTypeTable
  path="../../packages/api/src/types/quran-client.ts"
  name="QuranClientConfig"
/>
